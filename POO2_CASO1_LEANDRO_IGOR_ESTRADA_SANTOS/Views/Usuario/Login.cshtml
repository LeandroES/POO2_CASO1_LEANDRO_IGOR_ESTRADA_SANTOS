@{
    ViewBag.Title = "LoginPage";
}

@using (Html.BeginForm("Log", "Usuario"))
{
    @Html.Label("UserName:")
    @Html.TextBox("UserName")
    @Html.Label("Password:")
    @Html.TextBox("Password")
    <input type="submit" value="Login" />
}
